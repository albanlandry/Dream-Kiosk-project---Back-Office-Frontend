FROM node:20-alpine

WORKDIR /app

# 패키지 파일 복사 및 의존성 설치
COPY package*.json ./
RUN npm install

# 소스 코드는 볼륨으로 마운트되므로 여기서는 복사하지 않음
# 개발 모드에서는 파일 변경 감시를 위해 볼륨 마운트 사용

EXPOSE 3001

# 개발 모드에서는 dev 명령이 실행됨 (docker-compose에서 override)
CMD ["npm", "run", "dev"]

